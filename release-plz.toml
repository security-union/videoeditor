[workspace]
changelog_update = true
git_release_enable = false
git_tag_enable = false
semver_check = true
pr_branch_prefix = "release-plz-"
pr_labels = ["release"]
publish_timeout = "10m"

# The recorder UI never releases on its own — its trunk build ships inside
# videoeditor-record's committed ui/ dist.
[[package]]
name = "videoeditor-record-ui"
release = false

# The CLI crate is the user-facing release: it gets the GitHub release + tag.
[[package]]
name = "videoeditor"
git_release_enable = true
git_tag_enable = true
git_tag_name = "v{{ version }}"
git_release_name = "v{{ version }}"

[changelog]
protect_breaking_commits = true
