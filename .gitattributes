# Media through git-lfs (renders and audio stay untracked — see .gitignore;
# this covers committed static assets like example images)
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.webp filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text

# README demo strip stays plain git (GitHub won't render LFS images)
docs/*.jpg -filter -diff -merge text
